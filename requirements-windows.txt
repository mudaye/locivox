# Core STT Models (pre-built wheels available)
faster-whisper==1.0.3
openai-whisper==20231117

# Audio Processing (Windows-compatible wheels)
sounddevice==0.4.6
numpy==1.26.0
soundfile==0.12.1

# ML Backend - CPU only (pre-built wheels)
--index-url https://download.pytorch.org/whl/cpu
torch==2.1.0
torchaudio==2.1.0

# Utilities
pyyaml==6.0.1
colorama==0.4.6
tqdm==4.66.1

# Remove scipy - not needed for basic functionality
# scipy==1.11.4
